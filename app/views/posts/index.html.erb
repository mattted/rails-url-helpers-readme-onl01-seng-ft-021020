<% @posts.each do |post| %>
  <%# <div><a href='<%= "/posts/#{post.id}" %1>'><%= post.title %1></a></div> %> 
  <%# <div><%= link_to post.title, "/posts/#{post.id}" %1></div> %> 
  <%# <div><%= link_to post.title, post_path(post.id) %1></div> %> 
  <div><%= link_to post.title, post_path(post) %></div> 
<% end %>
